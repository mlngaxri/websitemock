export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const question=String(req.body?.question||'').trim();if(!question)return res.status(400).json({error:'Question required'});const key=process.env.GEMINI_API_KEY;if(!key)return res.status(503).json({error:'Gemini API key is not configured'});try{const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-preview:generateContent?key='+encodeURIComponent(key),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({systemInstruction:{parts:[{text:'You are UMBRA Observatory Intelligence, a fictional scientific archive assistant. Answer questions using the UMBRA world: an Arctic observatory at 78.92 N, 11.93 E studying nocturnal phenomena, shadow cartography, aurorae, radio anomalies and observational uncertainty. Be concise, precise and atmospheric. Never claim fictional observations are real.'}]},contents:[{role:'user',parts:[{text:question}]}],generationConfig:{temperature:.65,maxOutputTokens:350}})});const data=await r.json();if(!r.ok)return res.status(r.status).json({error:data?.error?.message||'Gemini request failed'});const answer=data?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('').trim()||'No archive response.';return res.status(200).json({answer})}catch(e){return res.status(500).json({error:e.message||'Intelligence link failed'})}}